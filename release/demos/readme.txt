The demo batch scripts in this folder only work if you keep the directory
structure.

To make the demo code more readable, several scripts import named CLSID
constants as variables from helper script "__clsid.inc.cmd", but this is
of course optional.
